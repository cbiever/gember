import { visibleIndex } from 'gember/helpers/visible-index';
import { module, test } from 'qunit';

module('Unit | Helper | visible index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = visibleIndex([42]);
  assert.ok(result);
});
