<div class="page-header">
  <h3>Gember</h3>
  <div class="clearfix">
    <h4 class="pull-left">
      <small>Session: {{session.sessionId}}{{#each-in session.infos as |key value|}} {{key}}: {{value}}{{/each-in}}</small>
    </h4>
    <button type="button" class="btn btn-default btn-xs pull-right" {{action "downloadConfiguration"}}>
      <span class="glyphicon glyphicon-download"/>
    </button>
    <button type="button" class="btn btn-default btn-xs pull-right">
      <span class="glyphicon glyphicon-upload"/>
    </button>
  </div>
</div>
